//% color="#0fbc11" weight=100 icon="\uf1eb"
namespace web {

    /**
     * Fetch text from a URL (requires custom runtime support)
     * @param url the URL to fetch from
     */
    //% block="fetch text from $url"
    export function fetchText(url: string): string {
        // Placeholder: the real implementation requires a modified MakeCode runtime
        return "Web access not supported in standard MakeCode Arcade"
    }

}
